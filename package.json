{
  "name": "mcp-validate-action",
  "version": "0.1.0",
  "description": "GitHub Action to validate MCP (Model Context Protocol) config files",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "node test/test.js",
    "lint": "eslint src"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MukundaKatta/mcp-validate-action.git"
  },
  "keywords": ["github-actions", "mcp", "model-context-protocol", "validator", "claude", "anthropic"],
  "author": "MukundaKatta",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
